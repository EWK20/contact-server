# Contact Server

Once the server receives input from contact form on the client side, it will send an email notification to a specified email address,
then it will store the data in the PostgreSQL database.
